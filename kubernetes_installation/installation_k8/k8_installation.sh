##### all nodes
# Open all requried firewall ports and configure security requirement.
# configure required network settings
# insatll and configure Docker
# insatll and configure kubernetes

###### on master node
# initialize the new kubernetes cluster (kubeadm init)
# install "calico" pod network plub-in

##### on each node
# joing the new cluster through "kubeadm join" command

###### on one of the nodes
# test the new cluster to ensure that all nodes, pods, and services are able to communicate
# deploy a new pod to help facilitate cluster test


